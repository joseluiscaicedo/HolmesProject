import React from 'react';
import '../assets/styles/components/Card.scss';

const CardIcon = (Icon) => (
  <>
    <img src={Icon} className='CardIcon' alt='img' />
  </>
);

export default CardIcon;
